apiVersion: v1
kind: Pod
metadata:
  name: nodehelloworld.example.com
  labels:
    app: helloworld
spec:
  containers:
  - name: dockersample
    image: ravindrayadavhere/dockersample
    ports:
    - name: nodejs-port
      containerPort: 3000
